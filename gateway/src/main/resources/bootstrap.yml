eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8000/eureka/

spring:
  cloud:
    config:
      label: master
      profile: test
      discovery:
        enabled: true
        service-id: config-server